---
import { Image } from "astro:assets";
import LinkOut from "~/assets/glyphs/link-out.svg";
import { links } from "~/data/links";

import NoBreak from "./NoBreak.astro";
---

<div class="not-a-button">
	SquiggleConf 2024 was a blast.
	<br />
	We're looking forward to seeing you next year on September 18th-19th!

	<br />
	<br />

	<div class="sub-mentions">
		<a
			class="sub-mention"
			href={links.nextYear}
			target="_blank"
			rel="noreferrer"
		>
			<strong>SquiggleConf 2025</strong>
			<Image alt="" class="link-out" src={LinkOut} />
		</a>
	</div>
</div>

<style>
	.not-a-button {
		background: var(--colorEmphasizedBackground);
		border-radius: 1rem;
		text-shadow: 0 3px 7px rgba(0, 0, 0, 0.5);
		color: var(--colorOffWhite);
		font-family: var(--fontFamilyHeading);
		font-size: var(--fontSizeBodyLarge);
		font-weight: 700;
		padding: 1.2rem 2rem 1rem;
		text-decoration: none;
	}

	.sub-mentions {
		align-items: center;
		display: flex;
		flex-wrap: wrap;
		gap: clamp(1rem, 5vw, 2rem);
		justify-content: center;
	}

	.sub-mention {
		color: var(--colorBlueLight);
		font-size: 2.5rem;
		font-family: var(--fontFamilyHeading);
		margin-top: 2rem;
		text-underline-offset: 0.2em;
		transition: var(--transitionMedium) color;
	}

	.sub-mention:hover {
		color: var(--colorWhite);
	}

	.link-out {
		height: 1rem;
		width: 1rem;
	}

	@media (min-width: 819px) {
		.sub-mentions {
			padding-bottom: 2rem;
		}
	}
</style>
